# m_load_env
# load and import/merge u-boot environment
#
# global varable
# - file name (`bootenv`): _StringValue_
# - load address (`krnl_addr`): _HexValue_
#
# input Parameter
# - storage class (`p_class`): mmc, usb, sata
# - storage device (`p_device`): _HexValue_
# - name prefix (`p_prefix`): _StringValue_
#
# return value
# - true: success / false: error 

p_filename=${p_prefix}/${p_conf}/${bootenv};
p_loadaddr=${krnl_addr};
if run m_load; then
  env import -t ${krnl_addr} ${filesize};
  if test ${dbglevel} > 0; then
    echo Importing environment from ${p_class}:${p_device}:${p_filename};
  fi;
  true;
else
  if test ${errlevel} > 0; then
    echo Importing environment from ${p_class}:${p_device}:${p_filename} failed!;
  fi;
  false;  
fi;
# vim:ft=sh
